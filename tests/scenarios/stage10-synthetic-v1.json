{
  "schema": "rtsim-software-baseline-v1",
  "provenance": "synthetic",
  "model_family": "planar contact presets, application 0.6.0",
  "generator": "examples/stage10_fixture.rs",
  "expected_motor_torque_scale": 1.1,
  "parameter_absolute_tolerance": 1e-12,
  "holdout_speed_rms_max_m_s": 1e-8,
  "required_coverage": 1.0,
  "basis": "Known synthetic generating parameter and same-model deterministic outputs. Thresholds fixed before execution, not based on physical instruments.",
  "physical_qualification": false
}
